# Create venv
    python -m venv venv

# Active venv
    venv\Scripts\activate

# install packages
    pip install -r requirements.txt

# install playwright
    playwright install    

# run app
    python app.py